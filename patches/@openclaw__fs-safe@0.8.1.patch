diff --git a/dist/native-staged-file.d.ts b/dist/native-staged-file.d.ts
index 7a4938e7fcb5c0fcdf60fca68e664bca9ce666c2..09c80731b15506ac95f9e672bea0b9a74d9f9a2a 100644
--- a/dist/native-staged-file.d.ts
+++ b/dist/native-staged-file.d.ts
@@ -7,8 +7,8 @@ export type NativeStagingBinding = NativeBinding & Required<Pick<NativeBinding,
 export declare function assertNativeStaging(binding: NativeBinding): asserts binding is NativeStagingBinding;
 declare class NativeStagedFile implements StagedFile {
     #private;
-    constructor(binding: NativeStagingBinding, parentFd: number, directory: StagedFileReceipt["directory"], portableNames: boolean, publishedMode: number);
-    static create(binding: NativeStagingBinding, parentFd: number, directory: StagedFileReceipt["directory"], input: PinnedWriteInput, mode: number, maxBytes?: number, portableNames?: boolean): Promise<NativeStagedFile>;
+    constructor(binding: NativeStagingBinding, parentFd: number, directory: StagedFileReceipt["directory"], portableNames: boolean, publishedMode: number, strictDurability: boolean);
+    static create(binding: NativeStagingBinding, parentFd: number, directory: StagedFileReceipt["directory"], input: PinnedWriteInput, mode: number, maxBytes?: number, portableNames?: boolean, strictDurability?: boolean): Promise<NativeStagedFile>;
     static write(binding: NativeStagingBinding, parentFd: number, directory: StagedFileReceipt["directory"], params: PinnedWriteParams, parentGuard: AnyAsyncDirectoryGuard): Promise<FileIdentityStat>;
     get receipt(): StagedFileReceipt;
     assertCurrent(): Promise<void>;
@@ -21,4 +21,4 @@ declare class NativeStagedFile implements StagedFile {
 export declare const createNativeStage: (...args: Parameters<typeof NativeStagedFile.create>) => Promise<StagedFile>;
 export declare const writeNativeStage: typeof NativeStagedFile.write;
 export {};
-//# sourceMappingURL=native-staged-file.d.ts.map
\ No newline at end of file
+// Fork staging contract: declarations updated by the pinned dependency patch.
diff --git a/dist/native-staged-file.js b/dist/native-staged-file.js
index b750165fb27d616a9d6353f0409c26cbc6757a5b..6e999937a690bdf398d962c8a532b657a9d59740 100644
--- a/dist/native-staged-file.js
+++ b/dist/native-staged-file.js
@@ -90,23 +90,25 @@ class NativeStagedFile {
     #directory;
     #portableNames;
     #publishedMode;
+    #strictDurability;
     #name = `.fs-safe-${randomUUID()}.tmp`;
     #state = { status: "open", publication: NOT_PUBLISHED };
     #receipt;
-    constructor(binding, parentFd, directory, portableNames, publishedMode) {
+    constructor(binding, parentFd, directory, portableNames, publishedMode, strictDurability) {
         this.#binding = binding;
         this.#parentFd = parentFd;
         this.#directory = directory;
         this.#portableNames = portableNames;
         this.#publishedMode = publishedMode;
+        this.#strictDurability = strictDurability;
     }
     // Takes ownership of parentFd, including all construction and preparation failures.
     static async create(binding, parentFd, directory, input, mode, maxBytes, 
     // Public staging uses portable names; existing POSIX writes accept literal names.
-    portableNames = true) {
+    portableNames = true, strictDurability = false) {
         let staged;
         try {
-            staged = new NativeStagedFile(binding, parentFd, directory, portableNames, mode);
+            staged = new NativeStagedFile(binding, parentFd, directory, portableNames, mode, strictDurability);
         }
         catch (error) {
             try {
@@ -174,6 +176,14 @@ class NativeStagedFile {
         assertStagedDirectoryCurrent(this.#directory);
         this.#assertNamed(this.#name);
     }
+    #sync(fd) {
+        if (this.#strictDurability) {
+            fs.fsyncSync(fd);
+        }
+        else {
+            syncNativeFileBestEffort(fd);
+        }
+    }
     async #prepare(input, maxBytes) {
         try {
             const mode = this.#publishedMode;
@@ -188,7 +198,7 @@ class NativeStagedFile {
             const fd = state.fileFd;
             fs.fchmodSync(fd, 0o600);
             await writeNativeInput(fd, input, maxBytes);
-            syncNativeFileBestEffort(fd);
+            this.#sync(fd);
             const stat = fs.fstatSync(fd, { bigint: true });
             this.#receipt = Object.freeze({
                 directory: this.#directory,
@@ -261,8 +271,8 @@ class NativeStagedFile {
             // fence. Mode changes use the owned fd, including for final mode 000.
             const fd = this.#file();
             fs.fchmodSync(fd, this.#publishedMode);
-            syncNativeFileBestEffort(fd);
-            syncNativeFileBestEffort(this.#parentFd);
+            this.#sync(fd);
+            this.#sync(this.#parentFd);
             this.#assertNamed(basename);
             assertStagedDirectoryCurrent(this.#directory);
             return receipt;
diff --git a/dist/native.d.ts b/dist/native.d.ts
index 8e741b1de9923605efa16d800da71f15435a084c..fdddd3d9d4d94c625188004da7785b276fae4810 100644
--- a/dist/native.d.ts
+++ b/dist/native.d.ts
@@ -7,8 +7,8 @@ export declare function __nativeLoaderDetectorsForTest(): {
     elfInterpreter: boolean | undefined;
 };
 export declare function __nativeTargetForTest(platform: NodeJS.Platform, arch: string, musl?: boolean): string | undefined;
-export declare function getNativeBinding(): NativeBinding | undefined;
-export declare function requireNativeBinding(): NativeBinding;
+export declare function getNativeBinding(requiredMode?: "require"): NativeBinding | undefined;
+export declare function requireNativeBinding(requiredMode?: "require"): NativeBinding;
 export declare function __setNativeLoaderForTest(loader: () => NativeBinding): void;
 export declare function __resetNativeLoaderForTest(): void;
-//# sourceMappingURL=native.d.ts.map
\ No newline at end of file
+// Fork staging contract: declarations updated by the pinned dependency patch.
diff --git a/dist/native.js b/dist/native.js
index fa0b8d8038537cb02191621dcc47496d75b7959d..6abcdc9562905f652056ac5ae4756e2feac3ae72 100644
--- a/dist/native.js
+++ b/dist/native.js
@@ -159,8 +159,8 @@ export function __nativeLoaderDetectorsForTest() {
 export function __nativeTargetForTest(platform, arch, musl = false) {
     return targetFor(platform, arch, musl);
 }
-export function getNativeBinding() {
-    const { mode } = getFsSafeNativeConfig();
+export function getNativeBinding(requiredMode) {
+    const mode = requiredMode ?? getFsSafeNativeConfig().mode;
     if (mode === "off")
         return undefined;
     if (!attempted) {
@@ -181,8 +181,8 @@ export function getNativeBinding() {
     }
     return undefined;
 }
-export function requireNativeBinding() {
-    const native = getNativeBinding();
+export function requireNativeBinding(requiredMode) {
+    const native = getNativeBinding(requiredMode);
     if (!native) {
         throw new FsSafeError("helper-unavailable", "native fs-safe helper is unavailable", {
             cause: loadError,
diff --git a/dist/staged-file.d.ts b/dist/staged-file.d.ts
index 048e34b8bba66a2f45ba428f152dd3350fdd881f..f9f37dcdd65ebf2cbc50d5b1eda01e1983180453 100644
--- a/dist/staged-file.d.ts
+++ b/dist/staged-file.d.ts
@@ -6,5 +6,9 @@ export declare function stageFileInDirectory(options: {
     content: string | Uint8Array;
     /** Published mode; the unpublished stage stays at 0600. Defaults to 0600. */
     mode?: number;
+    /** Require native staging for this operation without changing process configuration. */
+    nativeMode?: "require";
+    /** Propagate every file/directory sync failure, including EPERM. Defaults remain best-effort. */
+    durability?: "strict";
 }): Promise<StagedFile>;
-//# sourceMappingURL=staged-file.d.ts.map
\ No newline at end of file
+// Fork staging contract: declarations updated by the pinned dependency patch.
diff --git a/dist/staged-file.js b/dist/staged-file.js
index a27c4ef2d641b9f00fc534e7ee7b0e408c739490..4a2f25e1dff34a99ca70fab7a4802ea42fd31b88 100644
--- a/dist/staged-file.js
+++ b/dist/staged-file.js
@@ -3,13 +3,19 @@ import { assertNativeStaging, createNativeStage } from "./native-staged-file.js"
 import { requireNativeBinding } from "./native.js";
 import { openStagedDirectory } from "./staged-directory.js";
 export async function stageFileInDirectory(options) {
+    const nativeMode = options.nativeMode;
+    const durability = options.durability;
+    if ((nativeMode !== undefined && nativeMode !== "require") ||
+        (durability !== undefined && durability !== "strict")) {
+        throw new FsSafeError("invalid-path", "invalid per-operation staging policy");
+    }
     if (process.platform !== "linux" && process.platform !== "darwin") {
         throw new FsSafeError("unsupported-platform", "retained-directory staging requires Linux or macOS");
     }
-    const binding = requireNativeBinding();
+    const binding = requireNativeBinding(nativeMode);
     assertNativeStaging(binding);
     const input = { kind: "buffer", data: Buffer.from(options.content) };
     const mode = options.mode ?? 0o600;
     const parent = openStagedDirectory(options.directory);
-    return await createNativeStage(binding, parent.fd, parent.receipt, input, mode);
+    return await createNativeStage(binding, parent.fd, parent.receipt, input, mode, undefined, true, durability === "strict");
 }
